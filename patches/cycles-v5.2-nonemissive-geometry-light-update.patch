diff --git a/src/scene/geometry.cpp b/src/scene/geometry.cpp
index 90444c658..9f179695c 100644
--- a/src/scene/geometry.cpp
+++ b/src/scene/geometry.cpp
@@ -204,17 +204,22 @@ bool Geometry::has_motion_blur() const
 
 void Geometry::tag_update(Scene *scene, bool rebuild)
 {
+  bool uses_emission_sampling = false;
+  for (Node *node : used_shaders) {
+    Shader *shader = static_cast<Shader *>(node);
+    if (shader->emission_sampling != EMISSION_SAMPLING_NONE) {
+      uses_emission_sampling = true;
+      break;
+    }
+  }
+
   if (rebuild) {
     need_update_rebuild = true;
-    scene->light_manager->tag_update(scene, LightManager::MESH_NEED_REBUILD);
+    if (uses_emission_sampling) {
+      scene->light_manager->tag_update(scene, LightManager::MESH_NEED_REBUILD);
+    }
   }
-  else {
-    for (Node *node : used_shaders) {
-      Shader *shader = static_cast<Shader *>(node);
-      if (shader->emission_sampling != EMISSION_SAMPLING_NONE) {
-        scene->light_manager->tag_update(scene, LightManager::EMISSIVE_MESH_MODIFIED);
-        break;
-      }
-    }
+  else if (uses_emission_sampling) {
+    scene->light_manager->tag_update(scene, LightManager::EMISSIVE_MESH_MODIFIED);
   }
 
   scene->geometry_manager->tag_update(scene, GeometryManager::GEOMETRY_MODIFIED);
diff --git a/src/scene/object.cpp b/src/scene/object.cpp
index 83101feec..e643c1af7 100644
--- a/src/scene/object.cpp
+++ b/src/scene/object.cpp
@@ -1265,7 +1265,10 @@ void ObjectManager::tag_update(Scene *scene, const uint32_t flag)
     scene->geometry_manager->tag_update(scene, geometry_flag);
   }
 
-  scene->light_manager->tag_update(scene, LightManager::OBJECT_MANAGER);
+  /* Geometry already tags the light manager when an emissive surface changes. */
+  if ((flag & ~GEOMETRY_MANAGER) != 0) {
+    scene->light_manager->tag_update(scene, LightManager::OBJECT_MANAGER);
+  }
 
   /* Integrator's shadow catcher settings depends on object visibility settings. */
   if (flag & (OBJECT_ADDED | OBJECT_REMOVED | OBJECT_MODIFIED)) {
